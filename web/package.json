{
  "name": "cmyw-studio-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "触光工坊 Touch & Light — browser-side CMYW lithophane generator",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "tsc --noEmit && vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4",
    "wrangler": "^4.123.0"
  }
}
